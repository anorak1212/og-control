{
  "name": "og-control-tauri",
  "private": true,
  "version": "1.0.0",
  "description": "OG Control — Punto de venta e inventario de escritorio (Tauri + Rust + SQLite)",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
